import {  Controller,  Get,  NestMiddleware,  Param,  Query,  Req,} from '@nestjs/common';import { AppService } from './app.service';@Controller()export class AppController {  constructor(private readonly appService: AppService) {}  @Get('prop/:service/readd')  getHello(@Param('service') service: string, @Query() query: { status: string }): string {    return this.appService.getHello(service);  }}export class Middle implements NestMiddleware {  use(req: any, res: any, next: () => void): any {    console.log('lalala');    next();  }}